Example 1:
start
	request name from the user
	store name into a variable called "first_name"
	print "first_name" + "Hello, World!"
end

Example 2:
start
	request age from the user
	store age into a variable called "age"
	if "age" > or  == 18
		print "You're old enough"
	if "age" > or == 16 but < 18 
		print "Almost there"
	if "age" < 16
		print "You're just too young"
end
		
	